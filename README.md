# prime-number-identifier-
This is a simple C++ program that checks if a number is prime using the square root optimization

## How to use
1. Copy the code from `prime.cpp`.
2. Compile it using `g++ prime.cpp -o prime`.
3. Run it with `./prime`.
